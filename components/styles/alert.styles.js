import { makeStyles } from '@mui/styles';

export const alertStyles = makeStyles({
  container: {
    display: 'flex',
    justifyContent: 'center',
  },
  error: {
    position: 'fixed',
    top: 68,
  },
});
